import acceptanceCriteriaList from "./acceptanceCriteriaList";

describe("acceptanceCtiterialList.test", () => {
  it("should have a bunch of functions  ", () => {
    expect(acceptanceCriteriaList).toBeDefined();
  });
});
